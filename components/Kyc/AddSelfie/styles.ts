import { css } from "@emotion/react";

export const heading = css({
  fontSize: "16px",
  lineHeight: "32px",
  display: "flex",
  alignItems: "center",
  fontWeight: 500
})

export const allowContainer = css({
  height: "200px",
  width: "220px",
  backgroundColor: "white",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  cursor: "pointer"
})

export const cameraText = css({
  display: "flex",
  flexDirection: "column",
  alignItems: "center"
})

export const cameraContainer = css({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  gap: "20px"
})

export const webcamContainer = css({
  display: "flex"
})

export const selfieContainer = css({
  display: "flex",
  gap: "20px",
  alignItems: "center"
})

export const actionsContainer = css({

})

export const buttonContainer = css({
  display: "flex",
  justifyContent: "center",
  gap: "12px",
  marginTop: "20px"
})

export const crossImg = css({
  position: "absolute",
  top: -8,
  right: -10,
  cursor: "pointer"
})

export const backButton = css({
  backgroundColor: "white",
  color: "black",
  border: "1px solid #d9d9d9"
})